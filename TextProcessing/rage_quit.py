series = input()

output_line = ''
last_symbol = ''

# Loop through the string
for character in series:
    # alphabetic character
    if character.isalpha():
        print(character)
    else:
        print(character)